#include "main.h"
/**
* _isupper - checks if c is an uppercase letter
* @c: integer to test
* Return: 1 for true, 0 for false
*/
int _isdigit(int c)
{
	if (c >= 48 && c <= 57)
		return (1);
	return (0);
}
